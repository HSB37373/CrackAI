# 작업 내역 — 2026-08-09

## 개요

악성 민원인 이력 기억 시스템 및 상담원 통화 제한 패널티 기능 추가.

---

## 1. 악성 민원인 이력 기억 시스템

### 신규 파일

| 파일 | 설명 |
|------|------|
| `backend/caller_registry.py` | 민원인 이력 CRUD 모듈 |
| `data/blacklist.json` | 민원인 이력 영구 저장소 |

### 동작 방식

AI 상담으로 전환된 통화가 끝나면 해당 민원인의 이름·전화번호와 악성민원 이력이 `blacklist.json`에 자동 기록됩니다.

다음 통화 시 직원이 오른쪽 패널 **민원인 정보 조회** 란에 이름·전화번호를 입력하면 이력이 조회되며, 이력 횟수에 따라 욕설 경고 허용 횟수가 감소합니다.

| 6개월 내 이력 | 욕설 경고 허용 횟수 | AI 전환 조건 |
|:---:|:---:|---|
| 0회 (신규) | 3회 | 욕설 3번째 발화 시 |
| 1회 | 2회 | 욕설 2번째 발화 시 |
| 2회 | 1회 | 욕설 1번째 발화 시 |
| 3회 이상 | 0회 | 욕설 감지 즉시 |

### 추가된 API 엔드포인트

| 메서드 | 경로 | 설명 |
|--------|------|------|
| `POST` | `/caller/register` | 통화 세션에 민원인 정보 등록 |
| `GET` | `/caller/{phone}` | 전화번호로 이력 조회 |

### `/analyze` 응답 변경

기존 `risk_score >= 30` 조건 대신 **욕설 경고 횟수** 기반으로 AI 전환 조건 변경.

추가된 응답 필드:

| 필드 | 설명 |
|------|------|
| `profanity_warning_count` | 현재 통화에서 욕설 감지된 발화 수 |
| `ai_threshold` | 이 민원인에게 적용되는 욕설 허용 횟수 |
| `warnings_remaining` | AI 전환까지 남은 경고 횟수 |

### UI 변경

- 오른쪽 패널 상단에 민원인 정보 조회 입력란 추가 (이름, 전화번호, 조회 버튼)
- 이력 횟수에 따라 색상 배지 표시

  | 배지 색 | 조건 |
  |---------|------|
  | 🟢 초록 | 신규 민원인 |
  | 🟡 노랑 | 이력 1회 |
  | 🔴 빨강 | 이력 2회 |
  | 🔴 점멸 | 이력 3회 이상 |

- 통화 중 욕설 감지 시 채팅창에 경고 카운터 표시 ("경고 1/3 — 2회 남았습니다")
- 대시보드에 **욕설 경고 현황** 항목 추가 (실시간 업데이트)

---

## 2. 6개월 이력 만료

### 변경 파일

`backend/caller_registry.py`

### 동작 방식

이력 조회 시 `blacklist.json`의 전체 기록 중 **6개월(182일) 이내** 항목만 카운트합니다. 6개월이 지난 이력은 자동으로 무효 처리되어 경고 허용 횟수가 회복됩니다.

- 별도 삭제 작업 없음 — 조회 시마다 날짜 기준으로 필터링
- 원본 기록은 `history` 배열에 계속 보존 (감사 목적)

---

## 3. 상담원 통화 제한 패널티

### 동작 방식

AI로 전환되어 악성민원 이력이 기록되는 순간, 해당 민원인에게 **상담원 직접 통화 제한 시간**이 부여됩니다.

| 6개월 내 누적 이력 | 제한 시간 |
|:---:|:---:|
| 1회 | **1시간** |
| 2회 | **3시간** |
| 3회 이상 | **24시간** |

제한 시간 동안은 AI 챗봇 및 음성 상담만 이용 가능하며, 상담원과의 직접 통화는 불가합니다.

### 추가된 함수

| 함수 | 위치 | 설명 |
|------|------|------|
| `get_ban_status(phone)` | `caller_registry.py` | 현재 제한 여부와 남은 시간 반환 |
| `_ban_duration(count)` | `caller_registry.py` | 이력 횟수별 제한 시간 계산 |

`record_offense()` 호출 시 새 이력 포함 6개월 내 횟수를 계산하여 `ban_expires` 필드를 자동 기록합니다.

### UI 변경

민원인 조회 후 제한 중인 경우 배지 하단에 카운트다운 타이머 표시:

```
🚫 상담원 통화 제한 중
      23:47:12
  AI 챗봇 · 음성 상담만 가능
```

제한 시간이 만료되면 자동으로 아래로 전환:

```
✅ 통화 제한 해제됨
      00:00:00
  상담원 통화 재개 가능
```

---

## 변경된 파일 목록

| 파일 | 변경 유형 |
|------|----------|
| `backend/caller_registry.py` | 신규 생성 |
| `data/blacklist.json` | 신규 생성 |
| `main.py` | 수정 — 엔드포인트 2개 추가, `/analyze` 로직 변경 |
| `static/index.html` | 수정 — 민원인 조회 UI, ban 타이머 영역 추가 |
| `static/style.css` | 수정 — 조회 입력란, 배지, 타이머 스타일 추가 |
| `static/app.js` | 수정 — `registerCaller()`, `startBanCountdown()`, `clearBanStatus()`, 경고 카운터 로직 추가 |
